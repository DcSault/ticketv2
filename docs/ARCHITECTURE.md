# 🏗️ Architecture Technique

> **Dernière mise à jour**: 2026-01-16 - Création initiale

---

## Vue d'Ensemble

TicketV2 est une application full-stack composée de :
- **Frontend** : Single Page Application (SPA) React
- **Backend** : API REST Node.js/Express
- **Base de données** : PostgreSQL relationnelle

### Diagramme d'Architecture Global

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              CLIENT (Browser)                               │
│                                                                             │
│    ┌─────────────────────────────────────────────────────────────────────┐  │
│    │                      React SPA (Vite + TailwindCSS)                 │  │
│    │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐   │  │
│    │  │Dashboard │ │Statistics│ │  Admin   │ │ Archives │ │  Login   │   │  │
│    │  └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘   │  │
│    │       │            │            │            │            │          │  │
│    │  ┌────┴────────────┴────────────┴────────────┴────────────┴────┐    │  │
│    │  │                    Services Layer (Axios)                    │    │  │
│    │  │   authService │ callService │ adminService │ statisticsService│   │  │
│    │  └──────────────────────────────┬───────────────────────────────┘    │  │
│    └─────────────────────────────────┼───────────────────────────────────┘  │
└──────────────────────────────────────┼──────────────────────────────────────┘
                                       │ HTTP/REST (JWT Bearer)
                                       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                           SERVER (Node.js + Express)                        │
│                                                                             │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                        Middlewares Pipeline                           │  │
│  │   Helmet → CORS → Morgan → JSON Parser → Auth → TenantAccess          │  │
│  └──────────────────────────────────┬────────────────────────────────────┘  │
│                                     ▼                                       │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                            Routes Layer                               │  │
│  │  /api/auth │ /api/calls │ /api/statistics │ /api/admin │ /api/data    │  │
│  └──────────────────────────────────┬────────────────────────────────────┘  │
│                                     ▼                                       │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                         Controllers Layer                             │  │
│  │  authController │ callController │ statisticsController │ adminCtrl   │  │
│  └──────────────────────────────────┬────────────────────────────────────┘  │
│                                     ▼                                       │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                     Database Layer (pg Pool)                          │  │
│  └──────────────────────────────────┬────────────────────────────────────┘  │
│                                     │                                       │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                        Background Jobs                                │  │
│  │                    archiveOldCalls (30min)                            │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────┼──────────────────────────────────────┘
                                       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                          PostgreSQL 15                                      │
│   tenants │ users │ calls │ callers │ reasons │ tags │ call_tags           │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## Patterns Architecturaux

### 1. Layered Architecture (Backend)

```
┌─────────────────────────────────────┐
│           Routes Layer              │  ← Définition des endpoints
├─────────────────────────────────────┤
│         Middleware Layer            │  ← Auth, validation, logging
├─────────────────────────────────────┤
│        Controllers Layer            │  ← Logique métier
├─────────────────────────────────────┤
│          Database Layer             │  ← Accès PostgreSQL (Pool)
└─────────────────────────────────────┘
```

### 2. Service Pattern (Frontend)

```
┌─────────────────────────────────────┐
│         Pages (Components)          │  ← UI React
├─────────────────────────────────────┤
│          Services Layer             │  ← Abstraction API (Axios)
├─────────────────────────────────────┤
│         HTTP Common Layer           │  ← Config Axios, interceptors
└─────────────────────────────────────┘
```

### 3. Multi-Tenant Pattern

```
┌─────────────────────────────────────────────────────────────────┐
│                        Application                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │   Tenant A   │  │   Tenant B   │  │   Tenant C   │          │
│  │  (Infra)     │  │   (Dev)      │  │  (Support)   │          │
│  │              │  │              │  │              │          │
│  │ Users        │  │ Users        │  │ Users        │          │
│  │ Calls        │  │ Calls        │  │ Calls        │          │
│  │ Callers      │  │ Callers      │  │ Callers      │          │
│  │ Tags         │  │ Tags         │  │ Tags         │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │              Shared: global_admin, viewers                  ││
│  └─────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────┘
```

---

## Structure des Répertoires

```
ticketv2/
│
├── 📁 client/                      # ══════ FRONTEND ══════
│   ├── 📁 src/
│   │   ├── 📄 App.jsx              # Routing principal
│   │   ├── 📄 main.jsx             # Bootstrap React
│   │   ├── 📄 index.css            # Styles Tailwind
│   │   │
│   │   ├── 📁 pages/               # Composants de pages
│   │   │   ├── Admin.jsx           # Admin global
│   │   │   ├── AdminTenant.jsx     # Admin tenant
│   │   │   ├── Archives.jsx        # Gestion archives
│   │   │   ├── Dashboard.jsx       # Page principale
│   │   │   ├── DataManagement.jsx  # Gestion données
│   │   │   ├── ExportManager.jsx   # Export
│   │   │   ├── Home.jsx            # Accueil
│   │   │   ├── ImportManager.jsx   # Import
│   │   │   ├── Login.jsx           # Authentification
│   │   │   └── Statistics.jsx      # Statistiques
│   │   │
│   │   └── 📁 services/            # Couche API
│   │       ├── api.js              # Export centralisé
│   │       ├── http-common.js      # Config Axios
│   │       ├── authService.js      # Auth API
│   │       ├── callService.js      # Calls API
│   │       ├── adminService.js     # Admin API
│   │       └── statisticsService.js# Stats API
│   │
│   ├── 📄 index.html
│   ├── 📄 package.json
│   ├── 📄 vite.config.js
│   ├── 📄 tailwind.config.js
│   └── 📄 postcss.config.js
│
├── 📁 server/                      # ══════ BACKEND ══════
│   ├── 📄 index.js                 # Point d'entrée Express
│   │
│   ├── 📁 config/
│   │   └── database.js             # Pool PostgreSQL
│   │
│   ├── 📁 controllers/             # Logique métier
│   │   ├── authController.js
│   │   ├── callController.js
│   │   ├── adminController.js
│   │   ├── statisticsController.js
│   │   └── dataManagementController.js
│   │
│   ├── 📁 routes/                  # Définition routes
│   │   ├── auth.js
│   │   ├── calls.js
│   │   ├── admin.js
│   │   ├── statistics.js
│   │   └── dataManagement.js
│   │
│   ├── 📁 middleware/
│   │   └── auth.js                 # JWT, rôles, tenant
│   │
│   ├── 📁 jobs/
│   │   └── archiveOldCalls.js      # Job archivage auto
│   │
│   ├── 📁 scripts/
│   │   ├── setup-db.js             # Init BDD
│   │   └── migrate-add-no-password.js
│   │
│   └── 📁 utils/
│       └── logger.js               # Winston config
│
├── 📁 docs/                        # ══════ DOCUMENTATION ══════
│   ├── INDEX.md
│   ├── ARCHITECTURE.md             # ← CE FICHIER
│   ├── API_REFERENCE.md
│   ├── DATABASE.md
│   ├── FRONTEND.md
│   ├── BACKEND.md
│   ├── SECURITY.md
│   ├── DEPLOYMENT.md
│   ├── CONVENTIONS.md
│   └── TROUBLESHOOTING.md
│
├── 📄 package.json                 # Dépendances backend
├── 📄 Dockerfile
├── 📄 docker-compose.yml
├── 📄 docker-compose.local.yml
├── 📄 README.md
└── 📄 DOCKER.md
```

---

## Flux de Données

### Création d'un Appel

```
┌──────────┐    1. Submit Form     ┌──────────────┐
│  Client  │ ────────────────────► │  Dashboard   │
│ (Browser)│                       │   (React)    │
└──────────┘                       └──────┬───────┘
                                          │
                                   2. callService.createCall(data)
                                          │
                                          ▼
                                   ┌──────────────┐
                                   │ http-common  │
                                   │   (Axios)    │
                                   └──────┬───────┘
                                          │
                           3. POST /api/calls + JWT
                                          │
                                          ▼
┌─────────────────────────────────────────────────────────────────┐
│                         EXPRESS SERVER                          │
│                                                                 │
│  ┌──────────┐  ┌──────────────┐  ┌────────────────┐            │
│  │  helmet  │─►│authenticateToken│─►│ensureTenantAccess│        │
│  └──────────┘  └──────────────┘  └────────┬───────┘            │
│                                           │                     │
│                                    4. req.user = decoded JWT    │
│                                           │                     │
│                                           ▼                     │
│                                   ┌───────────────┐             │
│                                   │callController │             │
│                                   │ .createCall() │             │
│                                   └───────┬───────┘             │
│                                           │                     │
│                          5. BEGIN TRANSACTION                   │
│                                           │                     │
│  ┌────────────────────────────────────────┼────────────────┐    │
│  │                   PostgreSQL                            │    │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐     │    │
│  │  │INSERT caller│─►│INSERT call  │─►│INSERT tags  │     │    │
│  │  └─────────────┘  └─────────────┘  └─────────────┘     │    │
│  │                                                         │    │
│  │                     6. COMMIT                           │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                           │                     │
│                                    7. Return call object        │
└───────────────────────────────────────────┼─────────────────────┘
                                            │
                                            ▼
                                     ┌──────────────┐
                                     │   Response   │
                                     │ {call: {...}}│
                                     └──────┬───────┘
                                            │
                           8. Update state + UI
                                            │
                                            ▼
                                     ┌──────────────┐
                                     │  Dashboard   │
                                     │ (Re-render)  │
                                     └──────────────┘
```

### Flow d'Authentification

```
┌─────────┐  1. Login Form   ┌─────────┐
│  User   │ ───────────────► │  Login  │
│         │                  │  Page   │
└─────────┘                  └────┬────┘
                                  │
                      2. authService.login(user, pwd)
                                  │
                                  ▼
                           ┌──────────────┐
                           │POST /api/auth│
                           │    /login    │
                           └──────┬───────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────┐
│                    authController                    │
│                                                     │
│  3. SELECT user FROM users WHERE username = $1     │
│  4. bcrypt.compare(password, hash)                 │
│  5. jwt.sign({id, role, tenantId}, SECRET)         │
│                                                     │
└──────────────────────────┬──────────────────────────┘
                           │
                    6. Return {token, user}
                           │
                           ▼
                    ┌──────────────┐
                    │ authService  │
                    │              │
                    │ 7. localStorage.setItem('token')
                    │ 8. localStorage.setItem('user')
                    └──────┬───────┘
                           │
                    9. Navigate to '/'
                           │
                           ▼
                    ┌──────────────┐
                    │   Home Page  │
                    │ (Authenticated)
                    └──────────────┘
```

---

## Composants Techniques

### Backend - Express Pipeline

```javascript
// Ordre d'exécution des middlewares
app.use(helmet());           // 1. Headers de sécurité
app.use(cors());             // 2. CORS
app.use(morgan('dev'));      // 3. Logging HTTP
app.use(express.json());     // 4. Parse JSON body
app.use(express.urlencoded());// 5. Parse URL-encoded

// Routes avec middlewares spécifiques
app.use('/api/auth', authRoutes);  // Pas de auth middleware
app.use('/api/calls', 
  authenticateToken,               // 6. Vérifie JWT
  ensureTenantAccess,              // 7. Vérifie accès tenant
  callRoutes
);
```

### Frontend - React Component Lifecycle

```
┌─────────────────────────────────────────────────────────────────┐
│                       Dashboard Component                        │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │                    State (useState)                      │    │
│  │  • calls: []                                            │    │
│  │  • loading: true                                        │    │
│  │  • selectedTenant: 'all' | tenantId                     │    │
│  │  • formData: {...}                                      │    │
│  └─────────────────────────────────────────────────────────┘    │
│                              │                                   │
│                    useEffect (mount)                            │
│                              │                                   │
│  ┌───────────────────────────┼──────────────────────────────┐   │
│  │                           ▼                              │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │   │
│  │  │ loadTenants  │  │  loadCalls   │  │loadSuggestions│  │   │
│  │  └──────────────┘  └──────────────┘  └──────────────┘   │   │
│  │                     Parallel API Calls                   │   │
│  └──────────────────────────────────────────────────────────┘   │
│                              │                                   │
│                              ▼                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                        RENDER                            │   │
│  │  • Header + Navigation                                   │   │
│  │  • Tenant Selector (if admin/viewer)                     │   │
│  │  • Call Form                                             │   │
│  │  • Calls List (map over calls)                           │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

---

## Dépendances entre Modules

### Backend

```
server/index.js
    │
    ├── config/database.js ◄───────────────────┐
    │                                          │
    ├── routes/                                │
    │   ├── auth.js ──────► controllers/authController.js ──► database.js
    │   ├── calls.js ─────► controllers/callController.js ──► database.js
    │   ├── admin.js ─────► controllers/adminController.js ─► database.js
    │   ├── statistics.js ► controllers/statisticsController.js ► database.js
    │   └── dataManagement.js ► controllers/dataManagementController.js ─┘
    │
    ├── middleware/auth.js (JWT verification)
    │
    ├── jobs/archiveOldCalls.js ──────────────► database.js
    │
    └── utils/logger.js (Winston)
```

### Frontend

```
client/src/main.jsx
    │
    └── App.jsx
        │
        ├── services/
        │   ├── http-common.js (Axios instance)
        │   │       ▲
        │   │       │
        │   ├── authService.js ────────┤
        │   ├── callService.js ────────┤
        │   ├── adminService.js ───────┤
        │   └── statisticsService.js ──┘
        │
        └── pages/
            ├── Login.jsx ─────────► authService
            ├── Dashboard.jsx ─────► callService, adminService
            ├── Statistics.jsx ────► statisticsService
            ├── Admin.jsx ─────────► adminService
            └── Archives.jsx ──────► callService
```

---

## Points d'Extension

### Ajouter un Nouveau Module Backend

1. Créer `server/controllers/newController.js`
2. Créer `server/routes/new.js`
3. Enregistrer dans `server/index.js` :
   ```javascript
   const newRoutes = require('./routes/new');
   app.use('/api/new', authenticateToken, newRoutes);
   ```
4. Mettre à jour [API_REFERENCE.md](./API_REFERENCE.md)

### Ajouter une Nouvelle Page Frontend

1. Créer `client/src/pages/NewPage.jsx`
2. Ajouter la route dans `client/src/App.jsx` :
   ```jsx
   <Route path="/new" element={<ProtectedRoute><NewPage /></ProtectedRoute>} />
   ```
3. Si nécessaire, créer un service dans `client/src/services/`
4. Mettre à jour [FRONTEND.md](./FRONTEND.md)

---

## Voir Aussi

- [DATABASE.md](./DATABASE.md) - Schéma de base de données
- [BACKEND.md](./BACKEND.md) - Détails implementation backend
- [FRONTEND.md](./FRONTEND.md) - Détails implementation frontend
- [SECURITY.md](./SECURITY.md) - Sécurité et authentification
